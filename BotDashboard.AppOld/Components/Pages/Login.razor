@using BotDashboard.App.Components.Layout

@page "/login"

@layout NotAuthorizedLayout

<PageTitle>Login</PageTitle>

<!-- <MudContainer Class="justify-content-center mt-4">
    <EditForm Model="LoginModel" OnInvalidSubmit="OnValidSubmit">
        <DataAnnotationsValidator/>
        <MudGrid>
            <MudItem xs="12" sm="7">
                <MudCard>
                    <MudCardContent>
                        <MudTextField T="string" Required="true" @bind-Value="LoginDTO.Email" Label="Email"
                                      Class="mt-3"
                                      InputType="InputType.Email"/>
                        <MudTextField T="string" Required="true" @bind-Value="LoginDTO.Password" Label="Password"
                                      Class="mt-3)"
                                      InputType="InputType.Password"/>
                    </MudCardContent>
                    <MudCardActions>
                        <MudButton OnClick="HandleLogin" ButtonType="ButtonType.Submit" Variant="Variant.Filled"
                                   Color="Color.Tertiary"
                                   Class="ml-auto text-center col-md-6">Login
                        </MudButton>
                    </MudCardActions>
                </MudCard>
            </MudItem>
        </MudGrid>
    </EditForm>
</MudContainer> -->

<input @bind="userIdentifier" />
<button class="btn btn-primary" @onclick="SignIn">Sign in</button>


<AuthorizeView>
    <Authorized>
        <p>Hello, @context.User.Identity?.Name!</p>
    </Authorized>
    <NotAuthorized>
        <p>You're not authorized.</p>
    </NotAuthorized>
</AuthorizeView>
